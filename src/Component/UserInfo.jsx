import React, {Component} from 'react'

const UserInfo = (props) => {
  return (
    <div>
      <h4>{props.name}</h4>
      <h4>{props.name}</h4>
      <h4>{props.name}</h4>
      <h4>{props.name}</h4>
      

    </div>
  )
}

export default UserInfo